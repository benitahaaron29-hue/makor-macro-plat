# Makor Platform — environment template
# Copy this file to .env.local and populate as needed.
# Currently the prototype runs entirely client-side and requires no secrets.

# Example for future API integration:
# MAKOR_API_BASE_URL=https://api.makor.example.com
# MAKOR_API_KEY=
